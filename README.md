This is an agent from scratch or a manual loop tool calling agent where a free teir groq api is used to evaluate the user given through the command line interface as the message is being parse and extracted then being evaluated using the groq api then the result is being presented in the cli itself.

To run this agent 
```bash
python agent.py <"User_message">
```

There are requirements that is going do before using it are groq and dotenv
