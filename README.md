This is a agent from scratch that can solve the problem either in one call or loop call and presents the final result.
To run this agent 
```bash
python agent.py
```

There are requirements that is going do before using it are groq and dotenv
